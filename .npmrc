# Use bash for npm scripts so git-dependency prepare scripts work on Windows.
# Git for Windows ships bash.exe on PATH — this fixes the "node_modules/.bin/babel
# is not recognized" error when npm compiles mumble-client and mumble-client-websocket.
script-shell=bash
